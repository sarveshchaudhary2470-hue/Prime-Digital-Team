{
  "name": "prime-digital-fullstack",
  "version": "1.0.0",
  "description": "Unified build script for Prime Digital",
  "main": "backend/server.js",
  "scripts": {
    "build-all": "npm install && npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "cd backend && node server.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cloudinary": "^1.41.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.5.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.6.2",
    "multer": "^2.1.1",
    "multer-storage-cloudinary": "^4.0.0",
    "nodemailer": "^8.0.7"
  }
}
